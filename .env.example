# API (локально або в Docker)
DATABASE_URL=postgresql://app:app@localhost:5432/learning
JWT_SECRET=change-me-in-production
PORT=3000
# Кілька origin через кому (CORS)
FRONTEND_ORIGIN=http://localhost:5173,http://localhost:8080

# Web (лише для production build у Docker; у dev Vite проксує /api)
# VITE_API_URL=http://localhost:3000
